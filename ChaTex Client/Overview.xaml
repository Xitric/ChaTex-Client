<Window x:Class="ChaTex_Client.Overview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChaTex_Client"
        xmlns:BLayer="clr-namespace:IO.Swagger.Model;assembly=BusinessLayer"  
        mc:Ignorable="d"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"

        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        Title="Overview" Height="484.81" Width="655.696">

    <Grid>
        <StackPanel HorizontalAlignment="Left" Margin="10,10,0,9.5" Width="80">
            <Image Height="65" Margin="0,0,0,15" Source="res/placeholder_profile_photo.png" />
            <Button Content="Chats" d:LayoutOverrides="Height" BorderBrush="{x:Null}" BorderThickness="1" Margin="10,0" Style="{DynamicResource MaterialDesignFloatingActionButton}"/>
            <Button Content="Groups" d:LayoutOverrides="Height" BorderBrush="{x:Null}" BorderThickness="1" Margin="10,5,10,0" Style="{DynamicResource MaterialDesignFloatingActionButton}"/>
            <Button x:Name="newGroupBtn"  Content="New&#10;Group" d:LayoutOverrides="Height" BorderBrush="{x:Null}" BorderThickness="1" Margin="10,5,10,0" Style="{DynamicResource MaterialDesignFloatingActionButton}" Click="NewGroupBtn_Click"/>
        </StackPanel>

        <TreeView x:Name="TViewGroups" HorizontalAlignment="Left" Margin="90,0,0,0.5" Width="144" BorderBrush="#FFB1B1B1" BorderThickness="1,0">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type BLayer:GroupDTO}" ItemsSource="{Binding Channels}">
                    <StackPanel Orientation="Horizontal">
                        
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type BLayer:ChannelDTO}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </TreeView.Resources>
        </TreeView>

        <ScrollViewer Margin="239,10,10.5,72.5">
            <StackPanel x:Name="SP">
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Hej Lars! Du er seeeeej" />
                </DockPanel>
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Thanks ;)" DockPanel.Dock="Right" />
                </DockPanel>
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Så lidt så lidt Lars!!!1!" />
                </DockPanel>
            </StackPanel>
        </ScrollViewer>
        <DockPanel Margin="239,0,10.5,18.5" Height="32" VerticalAlignment="Bottom" LastChildFill="False">
            <Grid Width="306">
                <TextBox HorizontalContentAlignment="Stretch" Height="Auto" x:Name="MessageField" TextWrapping="Wrap" Width="Auto" Margin="0,0,0,0"/>
                <TextBlock IsHitTestVisible="False" Text="Type something here..." VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Foreground="DarkGray">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=MessageField}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <Button Content="Send" Height="Auto" Width="80" Margin="0,0,0,0" DockPanel.Dock="Right"/>

        </DockPanel>
    </Grid>
</Window>
