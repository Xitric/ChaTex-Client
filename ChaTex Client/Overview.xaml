<!--main window-->
<Window x:Class="ChaTex_Client.Overview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChaTex_Client"
        xmlns:BLayer="clr-namespace:IO.Swagger.Model;assembly=BusinessLayer"  
        mc:Ignorable="d"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        Icon="ChaTexIcon.ico"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        Title="ChaTex" Height="484.81" Width="655.696" >


    <!-- main background -->
    <Grid>

        <!-- main menu -->
        <StackPanel HorizontalAlignment="Left" Margin="10,10,0,9.5" Width="80">
            <Image Height="65" Margin="0,0,0,15"  Source="pictures/person/person-icon.png"  />
            <Button Content="Chats" d:LayoutOverrides="Height" BorderBrush="{x:Null}" BorderThickness="1" Margin="10,0" Style="{DynamicResource MaterialDesignFloatingActionButton}" Background="#FF002864"/>
            <Button Content="Group" d:LayoutOverrides="Height" BorderBrush="{x:Null}" BorderThickness="1" Margin="10,5,10,0" Style="{DynamicResource MaterialDesignFloatingActionButton}" Background="#FF002864"/>
        </StackPanel>

        <!-- group, channel and chat view  -->
        <TreeView SelectedItemChanged="ChannelSelectionChanged" x:Name="TViewGroups" HorizontalAlignment="Left" Margin="90,0,0,0.5" Width="144" BorderBrush="#FFB1B1B1" BorderThickness="1,0" Foreground="#FF002864">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type BLayer:GroupDTO}" ItemsSource="{Binding Channels}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type BLayer:ChannelDTO}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </TreeView.Resources>
        </TreeView>

        <!-- message display -->
        <ScrollViewer Margin="239,51,10.333,87.333" >
            <StackPanel x:Name="SP" Background="White"  >
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Hej Lars! Du er seeeeej" Background="#FFE4E4E4" />
                </DockPanel>
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Thanks ;)" DockPanel.Dock="Right" />
                </DockPanel>
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Så lidt så lidt Lars!!!1!"  />
                </DockPanel>
            </StackPanel>
        </ScrollViewer>
        <DockPanel Margin="239,0,10.5,18.5" Height="32" VerticalAlignment="Bottom" LastChildFill="False">



            <!-- write message textbox -->
            <Grid Width="290">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="230*"/>
                    <ColumnDefinition Width="23*"/>
                </Grid.ColumnDefinitions>
                <TextBox HorizontalContentAlignment="Stretch" Height="Auto" x:Name="MessageField" TextWrapping="Wrap" Width="Auto" Grid.ColumnSpan="2"/>
                <TextBlock IsHitTestVisible="False" Text="Type something here..." VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,8,0,7" Foreground="DarkGray" Height="17" Width="144">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=MessageField}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- send button -->

            <Button Content="Send" Height="Auto" Width="64" Background="#FF002864" BorderBrush="#FF002864" DockPanel.Dock="Right"/>

            <!-- ad file button-->
            <Button Height="Auto" Width="53" Margin="0,0,0,0" Background="Transparent" BorderBrush="Transparent">
                <StackPanel Width="23" >
                    <Image Source="pictures/button/paperclipIcon.png" Height="29" Margin="-12,0,-5.333,0"  />
                </StackPanel>
            </Button>



        </DockPanel>
    </Grid>
</Window>
