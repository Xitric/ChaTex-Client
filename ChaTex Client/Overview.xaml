<!--main window-->
<Window x:Class="ChaTex_Client.Overview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChaTex_Client"
        xmlns:BLayer="clr-namespace:IO.Swagger.Model;assembly=BusinessLayer"  
        mc:Ignorable="d"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        Icon="ChaTexIcon.ico"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        Title="ChaTex" Height="484.81" Width="655.696" >


    <!-- main background -->
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height=".7*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height=".7*" />
            <RowDefinition Height=".2*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.8*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width=".4*" />
            <ColumnDefinition Width=".1*" />
        </Grid.ColumnDefinitions>

        <!-- main menu -->

        <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="5">
            <Grid Height="454" Margin="0,0,0.333,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />

                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
				<Image Height="65" Grid.Row="0"   Source="pictures/person/person-icon.png"  />

                <Button Content="Chats"  Grid.Row="1" d:LayoutOverrides="Height" BorderBrush="{x:Null}"  BorderThickness="1"  Style="{DynamicResource MaterialDesignFloatingActionButton}" Background="#FF002864"/>
                <Button Content="Group" Grid.Row="2" d:LayoutOverrides="Height" BorderBrush="{x:Null}" BorderThickness="1"  Style="{DynamicResource MaterialDesignFloatingActionButton}" Background="#FF002864"/>
				<Button x:Name="newGroupBtn" Grid.Row="3" Content="New&#10;Group" d:LayoutOverrides="Height" BorderBrush="{x:Null}" BorderThickness="1" Style="{DynamicResource MaterialDesignFloatingActionButton}" Click="NewGroupBtn_Click"/>

            </Grid>
        </StackPanel>


        <!-- group, channel and chat view  -->
        <TreeView SelectedItemChanged="ChannelSelectionChanged" x:Name="TViewGroups" HorizontalAlignment="Left" Margin="90,0,0,0.5" Width="144" BorderBrush="#FFB1B1B1" BorderThickness="1,0" Foreground="#FF002864">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type BLayer:GroupDTO}"   ItemsSource="{Binding Channels}">
                    <StackPanel Orientation="Horizontal">
                        
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
                <DataTemplate  DataType="{x:Type BLayer:ChannelDTO}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </TreeView.Resources>
        </TreeView>

        <!-- message display -->
        <ScrollViewer Grid.Column="2" Grid.ColumnSpan="3" Grid.Row="1" Grid.RowSpan="2" >
            <StackPanel x:Name="SP" Background="White"  >
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Hej Lars! Du er seeeeej" Background="#FFE4E4E4" />
                </DockPanel>
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Thanks ;)" DockPanel.Dock="Right" />
                </DockPanel>
                <DockPanel Height="Auto" LastChildFill="False" Width="Auto">
                    <TextBlock Text="Så lidt så lidt Lars!!!1!"  />
                </DockPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- write message textbox -->
        <DockPanel  Grid.Column="2" Grid.ColumnSpan="4" Grid.Row="3" Height="50" VerticalAlignment="Bottom" LastChildFill="False">

            <Grid Width="253">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="230*"/>
                    <ColumnDefinition Width="23*"/>
                </Grid.ColumnDefinitions>
                <TextBox HorizontalContentAlignment="Stretch" Height="Auto" x:Name="MessageField" TextWrapping="Wrap" Width="Auto" BorderBrush="#FF002864"/>
                <TextBlock IsHitTestVisible="False" Text="Type something here..." VerticalAlignment="Center" HorizontalAlignment="Left"  Foreground="DarkGray" Height="17" Width="144">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=MessageField}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>



            <Grid Width="122">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.2*"/>
                </Grid.ColumnDefinitions>
            


            <!-- ad file button-->
                <Button Height="30" Grid.Column="0"  VerticalAlignment="Center" Background="Transparent" BorderBrush="Transparent"/>


                <!-- send button -->
                <Button Content="Send" Grid.Column="1" HorizontalAlignment="Right"  VerticalAlignment="Stretch" Background="#FF002864" BorderBrush="#FF002864" />

                
            </Grid>
        </DockPanel>

       

        <!--lable for chat or channel name-->
        <Label Name="MessageName" Grid.Column="2" Grid.ColumnSpan="2"  HorizontalAlignment="Center"  VerticalAlignment="Center"/>

        <!--channel edit button-->

        <Button Name="editButton" Grid.Column="4" Grid.Row="0" HorizontalAlignment="Center"  VerticalAlignment="Center" Background="Transparent" BorderBrush="Transparent" Click="editButton_Click">
            <StackPanel Width="23" >
                <Image Source="pictures/button/settingsIcon.png" Height="19" />
            </StackPanel>
        </Button>
    </Grid>
</Window>
